# Demo walkthrough

Run:

```bash
python -m ops_playbook_runner run examples/playbooks/disk-space-triage.yaml --dry-run
```

The command creates a timestamped report under `runs/`. The checked-in sample report is at `docs/assets/sample-report.md` so visitors can inspect the output without running the project.
